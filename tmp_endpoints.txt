package main

import (
	"fmt"
	"github.com/The-17/agentsecrets/pkg/api"
)

func main() {
	fmt.Println("=== AUTH ===")
	for k, v := range api endpointMap["auth"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== SECRETS ===")
	for k, v := range api endpointMap["secrets"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== PROJECTS ===")
	for k, v := range api endpointMap["projects"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== WORKSPACES ===")
	for k, v := range api endpointMap["workspaces"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== AGENTS ===")
	for k, v := range api endpointMap["agents"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== LOG ===")
	for k, v := range api endpointMap["log"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== AUDIT ===")
	for k, v := range api endpointMap["audit"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== TELEMETRY ===")
	for k, v := range api endpointMap["telemetry"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
	fmt.Println("\n=== USERS ===")
	for k, v := range api endpointMap["users"] {
		fmt.Printf("- %s: %s\n", k, v)
	}
}